#include "main.h"
/**
 * _islower - checks for lower case letter
 * @c : character is checked if it is lowercase or not
 *
 * Return: 0 or 1
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
