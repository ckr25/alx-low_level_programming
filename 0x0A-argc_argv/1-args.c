# include <stdio.h>
/**
 * main - prints the for the program
 * @argc: is a counter for argument for int
 * @argv: is a counter for value of the argument to char
 * Return: Always success
 **/
int main(int argc, *argv[])
{
	(void)argv;

	printf("%d\n", (argc - 1));
	return (0);
}
