#include <stdio.h>
/**
 * main - prints the name of the program
 * @argc: is an argument counter for int
 * @argv: is an counter value of the argument to char
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	void(argv);

	printf("%d\n", (argc - 1));
	return (0);
}
