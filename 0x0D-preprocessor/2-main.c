#include "main.h"
#include <stdio.h>

/**
 * main - writes a program that prints the nameof the file it was compiled
 * Return: Always 0
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
